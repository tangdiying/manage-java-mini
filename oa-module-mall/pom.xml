<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>oa</artifactId>
        <groupId>cn.tdywork.oa</groupId>
        <version>${revision}</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>oa-module-mall</artifactId>
    <packaging>pom</packaging>

    <name>${project.artifactId}</name>

    <description>
        商城大模块，由 product 商品、promotion 营销、trade 交易、statistics 统计等组成
    </description>
    <modules>
        <module>oa-module-product</module>
        <module>oa-module-promotion</module>
        <module>oa-module-trade</module>
        <module>oa-module-statistics</module>
        <!--
            特殊：为什么会有 oa-module-trade-api 呢？
                oa-module-promotion 和 oa-module-trade 之间相互循环依赖，所以抽出 oa-module-trade-api 模块，这样：
                1. oa-module-promotion 依赖 oa-module-trade-api
                2. oa-module-trade 依赖 oa-module-promotion
            从而不存在相互（循环）依赖，即 oa-module-trade => oa-module-promotion => oa-module-trade-api
         -->
        <module>oa-module-trade-api</module>
    </modules>

</project>
